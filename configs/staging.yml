application:
  environment: staging
  log_level: INFO

storage:
  mode: databricks
  root: /Volumes/novaretail_staging/platform/data

spark:
  app_name: NovaRetailStaging
  shuffle_partitions: 64

databricks:
  catalog: novaretail_staging
