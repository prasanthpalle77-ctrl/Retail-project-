application:
  environment: test
  log_level: WARNING

storage:
  root: tmp/test-data
  landing: tmp/test-data/landing
  bronze: tmp/test-data/warehouse/bronze
  silver: tmp/test-data/warehouse/silver
  gold: tmp/test-data/warehouse/gold
  quarantine: tmp/test-data/quarantine
  checkpoints: tmp/test-data/checkpoints
  documents: data/documents

spark:
  app_name: NovaRetailTest
  master: local[2]
  shuffle_partitions: 2

databricks:
  catalog: novaretail_test
